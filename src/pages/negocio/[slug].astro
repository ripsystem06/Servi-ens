---
export async function getStaticPaths() {
  const { getBusinesses } = await import('@/lib/data-loader');
  return (await getBusinesses()).map((b) => ({ params: { slug: b.slug } }));
}

const { slug } = Astro.params;
return Astro.redirect(`/servicio/${slug}`, 301);
---
